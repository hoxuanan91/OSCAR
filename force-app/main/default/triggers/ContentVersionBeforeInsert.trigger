trigger ContentVersionBeforeInsert on ContentVersion (before insert) {
 /* if(PAD.canTrigger('ContentVersionBeforeInsert')) 
    { */
  /*
    for (ContentVersion cv : Trigger.new) {
                String Description = 'Version ' + cv.VersionData + cv.PathOnClient;
                cv.Description__c = Description;
                
                System.debug('##Description :  ' + cv.PathOnClient + ' Description ' + Description + 'CV  ' + cv)  ;  
    }
*/

   /* } */
}